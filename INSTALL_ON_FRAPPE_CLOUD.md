# Installation on Frappe Cloud

1. Upload the contents of this folder directly to the root of your GitHub repository.
2. Confirm that `pyproject.toml` is visible on the repository home page.
3. In Frappe Cloud, add the GitHub app using the repository URL.
4. Choose branch `main`.
5. Deploy the bench.
6. Install the app `wafd_one` on the target site.
