# WAFD ONE 10.0.0 RC123

- Rebuilt Iftar Project Wizard so every new project starts with a clean form.
- Fixed Next / Previous / Create button event binding and added safe create error handling.
- Removed stale wizard field defaults that looked like values carried over from the previous project.
- Rebuilt Iftar Report Center with automatic recent-project selection, project summary, grouped document cards, and direct print/navigation actions.
- Clears Frappe cache on migrate so updated page JavaScript and CSS load immediately.
