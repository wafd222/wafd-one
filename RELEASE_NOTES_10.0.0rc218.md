# WAFD ONE 10.0.0rc218

- Replaces the legacy unhashed mobile navigation asset with Frappe bundle assets for browser cache busting.
- Uses a new back-control DOM id so stale Safari code cannot collide with the current control.
- Hides and removes every legacy RC214-RC217 back control on the role home screen.
- Adds a body-level home marker from the role-home page lifecycle and a bundled observer as a second guard.
- No business workflow, undertaking PDF, permissions, signature, stamp, or terms changes.
