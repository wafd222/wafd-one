# WAFD ONE 10.0.0 RC257

## Quotation table heading correction

- Replaced the ambiguous quotation item-table heading `يومي` with `عدد الوجبات`.
- Applied the correction consistently to the mobile preview and generated PDF.
- Added an idempotent migration to refresh the installed quotation print format.
- Preserved all RC256 quotation, menu-image, PDF, permissions and workflow behavior.

## Deployment

Deploy the application and run the site migration. Confirm the installed version is `10.0.0rc257`, then clear the site cache and reopen the quotation preview.
