# WAFD ONE 10.0.0 RC299

## إصلاح استعادة ترحيل RC298

- أضيفت وحدة Python المفقودة للـ DocType: `WAFD Iftar Supervisor Daily Owner`.
- عولج خطأ `ModuleNotFoundError` الذي كان يوقف **Migrate Site** عند مزامنة DocTypes الخاصة بإفطار الصائم.
- أضيف Patch آمن يعيد تحميل جدول أصحاب السفر اليومي ثم تقرير المشرف اليومي ويمسح الكاش.
- أضيف فحص آلي للحزمة يتأكد أن كل DocType قياسي يحتوي على ملف Python وملف `__init__.py` قبل إصدار أي ZIP جديد.
- لا يغيّر الإصلاح بيانات المشروع ولا يحذف السجلات، ويحافظ على جميع وظائف RC298 وشاشاته وصلاحياته.

## طريقة الاستعادة على Frappe Cloud

1. ارفع RC299 بدل RC298 في المستودع/التطبيق المرتبط بالموقع.
2. شغّل **Migrate Site** مرة واحدة.
3. بعد نجاحه شغّل **Build Assets** ثم **Clear Cache**.
4. افتح النظام من جديد واختبر شاشة إفطار الصائم.

لا حاجة لاستعادة نسخة احتياطية لمجرد هذا الخطأ؛ ترحيل RC298 توقف بسبب ملف برمجي مفقود، وRC299 يكمّل الترحيل بصورة آمنة.
