# WAFD ONE 10.0.0 RC337

## إصلاح ظهور رحلات السائق والمزامنة

- اعتُبرت إعادة إرسال عملية تخص رحلة موثقة سابقًا مزامنة ناجحة وآمنة بدل إيقاف الطابور.
- تُحذف العمليات القديمة المكررة من هاتف السائق بعد تأكيد الخادم.
- لا تمنع مشكلة في رحلة واحدة مزامنة الرحلات الأخرى أو تحديث قائمة الرحلات المسندة.
- لا يُنشئ الهاتف أكثر من عملية معلقة من النوع نفسه للرحلة نفسها.
- تُحدّث قائمة الرحلات من الخادم بعد المزامنة، فتظهر الرحلات الجديدة المسندة للسائق.
- أُعيد إصلاح الربط القديم بين سجل السائق وحساب المستخدم دون تغيير أي تعيين صحيح قائم.
- بقيت رحلات إفطار الصائم معزولة عن شاشة الرحلات العادية.

## بعد التثبيت

1. شغّل `bench migrate`.
2. شغّل `bench clear-cache` و`bench build --app wafd_one`.
3. افتح حساب السائق واضغط «مزامنة الآن» مرة واحدة.

