# WAFD ONE 10.0.0 RC350

نسخة تصحيح تثبيت عاجلة للإصدار RC349:

- نقل ملف ترحيل RC349 إلى المسار الصحيح القابل للاستيراد بواسطة Frappe.
- إصلاح خطأ `ModuleNotFoundError` الذي كان يوقف `Migrate Site`.
- الاحتفاظ بإصلاح ظهور توقيع المستلم في تسليم سائق إفطار الصائم.
- الاحتفاظ بالقائمة المختصرة داخل شاشات إفطار الصائم.
- إضافة اختبار شامل يتحقق من وجود ملفات جميع إدخالات `patches.txt` داخل الحزمة.
- لا تغيير على بيانات الموقع أو المستندات أو التعهدات أو بقية وحدات التطبيق.
