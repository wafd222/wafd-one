# WAFD ONE 10.0.0rc48

- Restores standard WAFD DocTypes after Frappe orphan cleanup before post-migration setup.
- Makes Madinah hotel catalogue installation safe when WAFD Hotel metadata is temporarily unavailable.
- Prevents contract total JavaScript from writing `services_subtotal` on non-contract forms.
- Aligns package and runtime version metadata.
