# WAFD ONE 2.9.0

- Fixed Frappe v16 workspace rendering by adding a stable `id` to every workspace block.
- Added two card blocks as a reliable navigation fallback in addition to the seven shortcuts.
- Added a new post-model-sync patch so already-migrated sites rebuild the workspace again.
- Added validation for block IDs, shortcut targets, and card blocks.
