# WAFD ONE 3.0.0

- Added a standard Desk Page dashboard that does not depend on experimental Workspace shortcut rendering in Frappe v16.
- Routed the app tile directly to `/app/wafd-one-dashboard`.
- Kept the Workspace as a secondary entry and added stable block IDs plus a visible dashboard link.
- Added a migration patch to sync the Page and rebuild the Workspace.
