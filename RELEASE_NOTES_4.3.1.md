# WAFD ONE 4.3.1

Reviewed workflow stability release.

- Open a new quality inspection as a populated draft instead of inserting an invalid Conditional record.
- Open a new loading record as a populated draft so vehicle and driver can be selected when project defaults are missing.
- Reuse the canonical packaging workflow endpoint and preserve compatibility with the previous endpoint.
- Revalidated Python, JavaScript, JSON, DocType controllers, field references, and archive integrity.
