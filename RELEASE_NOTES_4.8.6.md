# WAFD ONE 4.8.6

- Restores the administration console as a standard Frappe Desk Page with the guaranteed route `wafd-administration-console`.
- Keeps the Single DocType only as a compatibility fallback.
- Updates the WAFD ONE workspace shortcut to target the Page route.
- Adds a migration patch that synchronizes and validates the Page before rebuilding navigation.
