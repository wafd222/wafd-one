# WAFD ONE 5.1.1

- Full re-audit of the centralized print designer.
- Preserves intentional zero/unchecked settings during migrations.
- Adds safe normalization for partial installations.
- Validates font sizes, margins, and header line width.
- Scopes print CSS and improves optional closing rendering.
- Revalidates all Python, JSON, DocType field orders, patch paths, and packaged assets.
