# WAFD ONE 6.3.3
- Force-overwrites every Hotel Undertaking Print Format stored in the database.
- Sets the safe undertaking format as the default and fails migration if `get_single` remains.
- Installs and verifies all 400 reviewed Madinah hotel catalogue records after every migration.
- Never deletes existing hotel or operational data.
