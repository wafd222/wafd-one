[project]
name = "wafd_one"
authors = [
    { name = "Wafd Al-Madinah Catering Services", email = "wafd.almadinah@gmail.com" }
]
description = "WAFD ONE catering operations and finance application for Frappe"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0-dev,<17.0.0"
erpnext = ">=16.0.0-dev,<17.0.0"
