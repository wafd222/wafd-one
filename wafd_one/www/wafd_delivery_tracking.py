def get_context(context):
    context.no_cache = 1
    context.no_breadcrumbs = True
    context.title = "متابعة التسليم | WAFD ONE"
    context.metatags = {"robots": "noindex, nofollow, noarchive"}
    return context

